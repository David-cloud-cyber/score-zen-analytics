import { createFileRoute } from "@tanstack/react-router";
import { LockKeyhole, Settings, ShieldCheck } from "lucide-react";
import { AdminCard, AdminSection } from "@/components/AdminShell";
export const Route = createFileRoute("/admin/parametres")({ component: AdminSettingsPage });
function AdminSettingsPage() { return <AdminSection eyebrow="Gouvernance" title="Paramètres" description="Règles opérationnelles et permissions. Les secrets restent gérés hors de cette interface."><div className="grid gap-4 sm:grid-cols-2"><AdminCard><Settings className="size-5 text-brand" /><p className="mt-3 text-sm font-black">Permissions</p><p className="mt-1 text-xs leading-relaxed text-muted-foreground">Les rôles admin et owner sont vérifiés côté serveur sur chaque fonction. Aucun bouton ne contourne la vérification.</p></AdminCard><AdminCard><LockKeyhole className="size-5 text-brand" /><p className="mt-3 text-sm font-black">Secrets protégés</p><p className="mt-1 text-xs leading-relaxed text-muted-foreground">Les clés API-Football, OpenRouter, Supabase et Fapshi ne sont ni affichées, ni modifiables, ni exportables ici.</p></AdminCard><AdminCard><ShieldCheck className="size-5 text-brand" /><p className="mt-3 text-sm font-black">Actions critiques</p><p className="mt-1 text-xs leading-relaxed text-muted-foreground">Toute modification de rôle, crédit, remboursement ou anonymisation passe par une demande puis une validation owner distincte.</p></AdminCard></div></AdminSection>; }
